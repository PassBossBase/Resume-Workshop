import { EditorWorkspaceLoading } from "@/features/editor/editor-workspace-loading";

/** 仅用于视觉验收的工作台加载态预览页。 */
export default function EditorLoadingPreviewPage() {
  return <EditorWorkspaceLoading />;
}
